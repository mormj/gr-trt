id: trt_fft
label: fft
category: '[TensorRT]'
templates:
  imports: import trt
  make: trt.fft(${fft_size}, ${batch_size}, ${forward})
parameters:
- id: fft_size
  label: FFT Size
  dtype: int
- id: batch_size
  label: Batch Size
  default: '1'
  dtype: int
- id: forward
  label: Forward
  default: 'true'
  dtype: bool
inputs:
- label: in
  domain: stream
  dtype: complex
  vlen: 1
outputs:
- label: out
  domain: stream
  dtype: complex
  vlen: 1
file_format: 1
