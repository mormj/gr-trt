# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-trt
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Find CUDA and TensorRT libraries
########################################################################
find_package(CUDA REQUIRED)
find_package(TensorRT REQUIRED)
find_package(gnuradio-cuda)

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND trt_sources
    logger.cpp
    infer_impl.cc
)

set(trt_sources "${trt_sources}" PARENT_SCOPE)
if(NOT trt_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT trt_sources)

add_library(gnuradio-trt SHARED ${trt_sources})
target_link_libraries(gnuradio-trt gnuradio::gnuradio-runtime gnuradio::gnuradio-cuda
${TensorRT_LIBRARY}
${CUDA_CUDA_LIBRARY}   # CUDA Driver API
${CUDA_CUDART_LIBRARY} # CUDA Runtime API
${CUDA_CUFFT_LIBRARIES}
${nvonnxparser_LIBRARY}
)
target_include_directories(gnuradio-trt
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
    ${CUDA_INCLUDE_DIRS} 
    ${TensorRT_INCLUDE_DIR}
  )
set_target_properties(gnuradio-trt PROPERTIES DEFINE_SYMBOL "gnuradio_trt_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-trt PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-trt)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_trt_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-trt)

if(NOT test_trt_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_trt_sources)

foreach(qa_file ${test_trt_sources})
    GR_ADD_CPP_TEST("trt_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
